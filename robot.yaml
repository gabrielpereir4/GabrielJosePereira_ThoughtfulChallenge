tasks:
  Trigger start script:
    shell: python -m robocorp.tasks run main.py -t main 

variables:
  search_phrase: "Test"
  news_category: "None"
  months: 2

condaConfigFile: conda.yaml
artifactsDir: output

ignoreFiles:
  - .gitignore